{
    "private": true,
    "type": "module",
    "scripts": {
        "start": "dotnet tool restore && dotnet fable watch src --runFast vite",
        "build": "dotnet tool restore && dotnet fable src --run vite build",
        "build-single-file": "dotnet tool restore && dotnet fable src --run vite build --mode single-file",
        "clean": "dotnet fable clean --yes && rimraf dist/ dist-single-file/",
        "pretest": "dotnet fable tests",
        "test": "mocha tests"
    },
    "dependencies": {
        "@monaco-editor/react": "^4.5.1",
        "@uiw/color-convert": "^1.3.0",
        "@uiw/react-color-colorful": "^1.3.0",
        "bulma": "^0.9.4",
        "monaco-editor": "^0.39.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.0.0",
        "mocha": "^10.2.0",
        "rimraf": "^5.0.1",
        "sass": "^1.63.3",
        "vite": "^4.3.9",
        "vite-plugin-singlefile": "^0.13.5"
    },
    "engines": {
        "node": ">=18"
    }
}
