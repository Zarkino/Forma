{
    "private": true,
    "scripts": {
        "start": "dotnet tool restore && dotnet fable watch src --runFast vite",
        "build": "dotnet tool restore && dotnet fable src --run vite build",
        "build-single-file": "dotnet tool restore && dotnet fable src --run vite build --mode single-file",
        "clean": "dotnet fable clean src --yes && rimraf dist/ dist-single-file/"
    },
    "dependencies": {
        "@monaco-editor/react": "^4.4.6",
        "bulma": "^0.9.4",
        "bulma-switch": "^2.0.4",
        "monaco-editor": "^0.35.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^3.1.0",
        "rimraf": "^5.0.0",
        "sass": "^1.58.0",
        "vite": "^4.1.0",
        "vite-plugin-singlefile": "^0.13.5"
    },
    "engines": {
        "node": ">=18"
    }
}
